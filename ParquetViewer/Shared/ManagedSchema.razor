@using Parquet.Schema;

@if(Schema != null)
{
    <ManagedSchemaLevel Fields=@Schema.Fields/>
}
else
{
    <strong>no schema</strong>
}

@code {

    [Parameter]
    public ParquetSchema? Schema { get; set; }
}
